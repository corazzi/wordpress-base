// All of your imported files should be encapsulated with an IIFE like below,
// and you should also add a semicolon before it starts to avoid any issues when
// the files are concatenated together.
;(function () {
    // This is where your code should go.
})();